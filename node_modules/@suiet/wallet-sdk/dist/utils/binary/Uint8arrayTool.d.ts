export declare class Uint8arrayTool {
    static toHex(bytes: Uint8Array): string;
    static ensureUint8Array(value: string | Uint8Array | number[]): Uint8Array;
    static bytesEqual(a: Uint8Array, b: Uint8Array): boolean;
}
